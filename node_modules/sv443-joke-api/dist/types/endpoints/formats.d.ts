import { Response } from "node-fetch";
import { BaseRequestOptions } from "./helper";
export interface FormatReqOptions extends BaseRequestOptions {
}
/**
 * Fetches available response formats on the api
 */
export declare function getFormats(options?: FormatReqOptions): Promise<object | Response>;
