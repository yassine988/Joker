import { Response } from "node-fetch";
import { BaseRequestOptions } from "./helper";
export interface CategoriesReqOptions extends BaseRequestOptions {
}
/**
 * Fetches available categories on the api
 */
export declare function getCategories(options: CategoriesReqOptions): Promise<object | Response>;
