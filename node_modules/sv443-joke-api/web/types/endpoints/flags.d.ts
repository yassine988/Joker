import { Response } from "node-fetch";
import { BaseRequestOptions } from "./helper";
export interface FlagsReqOptions extends BaseRequestOptions {
}
/**
 * Fetches available flags on the api
 */
export declare function getFlags(options?: FlagsReqOptions): Promise<object | Response>;
